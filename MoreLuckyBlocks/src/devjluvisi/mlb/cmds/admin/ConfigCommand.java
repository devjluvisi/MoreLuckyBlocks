package devjluvisi.mlb.cmds.admin;

/**
 * TODO: Move the /mlb reload and /mlb save commands into this command.
 *
 * Purpose: Allows the user to directly interact with managing the config files.
 * Commands: "/mlb config reload" -> Reloads any data in the config files on the
 * server, updates the lucky blocks relative to the blocks.yml. "/mlb config
 * save" -> Saves all of the current edited data on the server into the config
 * files. "/mlb config regen" -> Generates default config files. "/mlb config
 * help" -> View help relating to the config files.
 *
 * @author jacob
 *
 */
public class ConfigCommand {

}
